- name: Build with Gradle
  run: |
    # Force fix permissions on the fly
    find . -name "gradlew" -exec chmod +x {} +
    # Find the right directory and build
    PROJECT_DIR=$(find . -name "gradlew" -printf '%h' -quit)
    cd "$PROJECT_DIR" && ./gradlew assembleDebug
